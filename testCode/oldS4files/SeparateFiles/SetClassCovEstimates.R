setClass("cov.estimates",
         slots = list(sia.cov = "numeric",
                        sia.min.age = "numeric",
                        sia.max.age = "numeric",
                        MR1.cov = "numeric",
                        MR2.cov = "numeric",
                        inaccessible.prop = "numeric"
         ))
