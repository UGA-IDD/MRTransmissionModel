% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GetMeaslesSeroprevalence.per.TimePoint.R
\name{GetMeaslesSeroprevalence.per.TimePoint}
\alias{GetMeaslesSeroprevalence.per.TimePoint}
\title{Age-specific measles seroprevalence at requested simulation time points}
\usage{
GetMeaslesSeroprevalence.per.TimePoint(
  res,
  trans,
  epi.state,
  no.gens.in.year,
  time.point.index,
  age0is6to11monly = FALSE
)
}
\arguments{
\item{res}{numeric matrix. Full result matrix from an MSIRV simulation
(\code{n.states x n.timesteps}).}

\item{trans}{transition object containing \code{age.class} and
\code{epi.class} slots.}

\item{epi.state}{integer vector of length \code{nrow(res)} giving the
epidemiological state index (1 = M, 2 = S, 3 = I, 4 = R, 5 = V) for
each row of \code{res}.}

\item{no.gens.in.year}{numeric. Number of model generations per year
(not currently used internally but kept for interface compatibility).}

\item{time.point.index}{integer vector. Column indices of \code{res} at
which seroprevalence should be extracted (values in \code{1:ncol(res)}).
Only these columns are processed.}

\item{age0is6to11monly}{logical. If \code{TRUE}, the age-0 row (< 1 year)
is replaced by the sum of age classes 7-12 (months 6-11, 0-indexed), the
window after maternal antibody waning when seroprevalence is meaningful.}
}
\value{
A named list with three matrices, each of dimension
  \code{n.age.years x length(time.point.index)}, with columns named by
  \code{time.point.index}:
\describe{
  \item{imm.pop}{immune population (M + R + V) by single-year age group}
  \item{pop.age}{total population by single-year age group}
  \item{seroprev.age}{seroprevalence (imm.pop / pop.age); set to 1 where
    pop.age is zero}
}
}
\description{
Extracts immune (M + R + V) and total population counts from an MSIRV
simulation result matrix, aggregates them from monthly to annual age groups,
and returns seroprevalence only at the time points requested. Only
\code{time.point.index} columns are processed; the rest of the result matrix
is never touched.
}
